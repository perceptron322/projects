# Distributed CNN for MNIST

Проект демонстрирует **распределённое обучение глубокой сверточной нейронной сети** для распознавания рукописных цифр (датасет **MNIST**) с использованием **PyTorch Distributed Data Parallel (DDP)**.

## 🚀 Описание
Реализована классическая CNN-архитектура с двумя сверточными слоями и полносвязной частью. Обучение и тестирование выполняются в распределённом режиме с синхронизацией градиентов между процессами.

Поддерживается запуск:
- на одном узле с несколькими процессами  
- в кластере под управлением **SLURM**

## 🧠 Архитектура модели
- 2 × Conv2D + ReLU  
- MaxPooling  
- Dropout  
- 2 × Fully Connected  
- Softmax-классификация на 10 классов

## ⚙️ Технологии
- Python
- PyTorch
- torch.distributed (DDP)
- torchvision (MNIST)
- SLURM (опционально)

## ▶️ Запуск
Перед запуском убедитесь, что датасет MNIST загружен в папку `./data`.

Пример локального запуска с несколькими процессами:
```bash
torchrun --nproc_per_node=4 main.py